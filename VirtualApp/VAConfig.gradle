ext {
    if (project.hasProperty('appid')) {
        PACKAGE_NAME_32BIT = appid
    } else {
        PACKAGE_NAME_32BIT = 'io.busniess.va'
    }

    PACKAGE_NAME_ARM64 = 'io.virtualapp.addon.arm64'
    PERMISSION_PREFIX = PACKAGE_NAME_32BIT
    AUTHorti_PREFIX = PACKAGE_NAME_ARM64
    Bit=64 #不是128是我没想到的 
    VERSION = '3.3.0'
    VERSION_CODE = 180080914
}
